\name{add.maxima.printf}
\alias{add.maxima.printf}
\title{txt is a vector of maxima commmands
each line is a command without ;  at the end}
\usage{
  add.maxima.printf(txt)
}
\description{
  txt is a vector of maxima commmands each line is a
  command without ; at the end
}

